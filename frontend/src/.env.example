# Copy to .env.local (or .env) and point this at your deployed backend when
# building for production. Not needed for local dev — it defaults to
# http://localhost:4000/api
VITE_API_URL=http://localhost:4000/api
